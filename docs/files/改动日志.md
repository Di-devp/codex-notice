# 改动日志

## 2026-06-11 Notice Hook、红绿灯小组件与配置修复

### 改动内容
- `src-tauri/src/hooks/mod.rs`：Notice 管理的 Codex hooks 事件列表包含 `SessionStart`、`UserPromptSubmit`、`PermissionRequest`、`PreToolUse`、`PostToolUse`、`Stop`。`status()` 现在会检查管理块是否完整；如果旧配置缺少 `UserPromptSubmit`，返回“需要重新安装”，避免误判为已安装。
- `src-tauri/capabilities/default.json`：为 `main` 与 `traffic-widget` capability 增加 `core:window:allow-start-dragging`，修复小组件调用 `getCurrentWindow().startDragging()` 时被 Tauri 2 权限系统拒绝的问题。
- `src/App.vue`：红绿灯小组件使用 `getCurrentWindow().startDragging()` 进行窗口拖动；运行态使用 `traffic-running` 类让红、黄、绿三灯按跑马灯动画轮询。
- `src-tauri/src/storage/mod.rs`：红绿灯状态基于最近 30 分钟的会话事件计算。失败优先于审批，审批优先于运行，全部完成后才显示绿色。
- `src-tauri/src/server/mod.rs`：使用 `UserPromptSubmit` 记录 `TASK_START`，使用 `Stop` 记录本轮任务结束；普通工具成功事件不再作为用户关心的运行状态展示。

### 验证情况
- `pnpm build` 通过。
- `cargo test --manifest-path src-tauri/Cargo.toml` 通过，当前 22 个 Rust 测试全部通过。
- `pnpm tauri build --debug` 通过，产物位于 `src-tauri/target/debug/bundle/macos/Notice.app`。
- 重启 Notice 后，`curl -sS http://127.0.0.1:3746/health` 返回 `ok`。

### 迁移说明
- 当前用户机器的 `~/.codex/config.toml` 旧 Notice 管理块缺少 `UserPromptSubmit`。需要在 Notice 的 Providers 页面重新点击安装 Codex Hooks，随后新开 Codex 会话或重启 Codex 客户端测试红绿灯运行态。

## 2026-06-16 Codex 剩余用量展示与小组件裁切修复

### 改动内容
- `src-tauri/src/codex_usage.rs`：新增 Codex 本机用量采集器，扫描 `~/.codex/sessions/**/*.jsonl` 与 `~/.codex/archived_sessions/**/*.jsonl`，只解析包含 `rate_limits` 的 `token_count` 事件，并缓存 15 秒，避免小组件高频轮询时反复扫盘。
- `src-tauri/src/domain/mod.rs`：扩展 `TrafficWidgetStatus`，新增 `codex_usage: Option<CodexUsageStatus>`；`CodexUsageWindow` 同时保存 `used_percent`、`remaining_percent`、`window_minutes`、`resets_at`。
- `src-tauri/src/storage/mod.rs`：`traffic_widget_status()` 在原有红黄绿/跑马灯状态基础上附加最近一次 Codex 用量快照。
- `src/types.ts` 与 `src/App.vue`：前端同步 `codexUsage` 类型，小组件第二行展示 `5h 100% · 7d 100%` 这类剩余用量摘要；Dashboard 的 Traffic Widget 卡片也展示用量摘要。
- `src/styles.css`：修复小组件根容器 `body.widget-body #app` 仍为旧 `164x66` 的问题，更新为 `232x76`。
- `src-tauri/src/commands/mod.rs`：提取小组件尺寸常量；新建窗口使用 `232x76`；复用已存在的 `traffic-widget` 窗口时调用 `set_min_size`、`set_max_size`、`set_size` 强制更新尺寸，避免旧运行实例沿用历史窗口大小。

### 验证情况
- `pnpm build` 通过；仅有 Vite chunk 大小提示。
- `cargo test` 通过，33 个 Rust 测试全部通过。
- `pnpm tauri:build` 在沙箱内会卡在 DMG 的 `bundle_dmg.sh` 阶段；使用非沙箱权限重跑后成功生成：
  - `src-tauri/target/release/bundle/macos/Notice.app`
  - `src-tauri/target/release/bundle/dmg/Notice_1.0.0_aarch64.dmg`

### 迁移说明
- 如果桌面上仍显示旧尺寸小组件，需要退出当前运行中的 Notice，再打开新构建的 `Notice.app` 或安装新的 DMG。已有窗口复用逻辑已会在下次显示小组件时强制 resize。

## 2026-06-16 项目文档与检索上下文同步

### 改动内容
- `docs/files/改动日志.md`、`docs/files/用户操作手册.md`、`docs/files/错题本.md`、`docs/files/项目记忆.md`、`docs/files/项目需求文档.md`：补充 Codex 剩余用量展示、小组件裁切修复、打包产物和验证结果。
- `.understand-anything/.understandignore`：新增 `.codegraph/` 与 `.tmp/`，避免 understand-anything 扫描本地代码图缓存和 clang module cache。
- `.understand-anything/intermediate/scan-result.json`、`import-map.json`、`batches.json` 与 `fingerprints.json`：使用 understand-anything 插件的确定性脚本重建扫描、导入关系、批次和结构指纹上下文。

### 验证情况
- `scan-project.mjs` 重跑后扫描 88 个项目文件，过滤 100 个生成缓存文件，复杂度为 `moderate`。
- `extract-import-map.mjs` 识别 19 个含 import 的文件与 45 条内部 import 边。
- `compute-batches.mjs` 生成 11 个批次。
- `build-fingerprints.mjs` 重建 30 个 Rust/TypeScript/JavaScript 源文件的结构指纹。

### 限制说明
- 当前 Codex 工具环境没有暴露 `/understand-anything:understand <project-root> --language zh` 或等价完整 runner，因此本次只刷新了确定性扫描/指纹上下文；`knowledge-graph.json` 的语义节点仍需在可用 understand-anything runner 中完整重建。

## 2026-06-18 多平台 Git 同步

### 改动内容
- 将当前 Notice 工作区有效改动提交为 `e46e015 Update Notice traffic widget and promo assets`。
- 提交内容包括红绿灯小组件、Codex 用量采集、Mochi 桌宠同步、项目文档与宣传素材等当前项目文件。
- 未提交本地缓存目录：`.codegraph/`、`.tmp/`。

### 验证情况
- `pnpm build` 通过；仅保留 Vite chunk size warning。
- `cargo test --manifest-path src-tauri/Cargo.toml` 通过，47 个 Rust 测试全部通过。

### 推送结果
- GitHub：`origin/main` 已推送到 `e46e015`。
- GitCode：`gitcode/main` 已推送到 `e46e015`。
- Gitee：`gitee/main` 已推送到 `e46e015`。

### 凭据说明
- Gitee 远端为 `git@gitee.com:www2333956/codex-notice.git`。
- 对应本机公钥指纹：`SHA256:3eGL4HP28igtKjVthO7rE9jqSbUs5tZTcCzkTYeSMCk`。
- 对应本机私钥路径：`~/.ssh/codex_notice_gitee`。

## 2026-06-18 项目文档与检索上下文增量同步

### 改动内容
- `docs/files/改动日志.md`、`docs/files/用户操作手册.md`、`docs/files/错题本.md`、`docs/files/项目记忆.md`、`docs/files/项目需求文档.md`：补充本轮多平台 Git 同步、Gitee SSH key 使用方式与认证故障处理。
- 按用户要求，不创建 `docs/files/AgentScope Java 2.0 知识沉淀.md`；Notice 当前保留 5 个项目文档类目。

### 检索上下文刷新
- 已确认官方 Understand-Anything 本地 checkout：`/Users/di/Documents/CodexWorkspace/Understand-Anything`。
- `.understand-anything/config.json` 保持中文输出配置：`{"outputLanguage":"zh"}`。
- 使用 understand-anything 插件脚本刷新 deterministic 上下文：
  - `scan-project.mjs`：扫描 88 个文件，过滤 100 个生成/缓存文件，复杂度 `moderate`。
  - `extract-import-map.mjs`：扫描 88 个文件，识别 19 个含 import 的文件与 45 条 import 边。
  - `compute-batches.mjs`：生成 9 个批次。
  - `build-fingerprints.mjs`：重建 31 个文件的 fingerprints baseline。

### 限制说明
- 当前 Codex 工具环境仍未暴露完整 `/understand-anything:understand /Users/di/Documents/Notice --language zh` runner；本次刷新的是 deterministic scan/import/batch/fingerprint 层。

## 2026-06-18 Notice 与 Mochi 桌宠显示适配

### 来源会话
- 参考 Codex 会话：`019eace5-f1dc-7343-ad23-10db25a2c7d3`
- 会话工作目录：`/Users/di/Documents/DiWorkspace/mochi`
- Mochi 固件路径：`/Users/di/Documents/DiWorkspace/mochi/clawd-mochi-main/clawd_mochi/clawd_mochi.ino`

### Notice 改动内容
- `src-tauri/src/pet.rs`：桌宠同步请求改为向 Mochi 发送 `state`、`quota`、`quota2`，目标接口为 `/pet/status`；`quota` / `quota2` 分别承载 5h 与 7d 余量文案，例如 `5h 88%`、`7d 41%`。
- `src-tauri/src/pet.rs`：新增 `pet_last_payload` 去重保护，`state|quota|quota2` 与上次一致时跳过网络请求，避免 Notice 轮询或重复事件导致 Mochi 屏幕重复刷新。
- `src-tauri/src/storage/mod.rs`：新增 `codex_usage_current` 设置项，持久化当前接受的 Codex 余量快照；`traffic_widget_status_snapshot()` 只在余量签名变化时标记 `codex_usage_changed`。
- `src-tauri/src/commands/mod.rs`：`get_traffic_widget_status()` 与 `refresh_runtime_status()` 获取状态快照后，只有 `codex_usage_changed` 为 true 时才触发 `pet::sync_status()`。
- `src-tauri/src/storage/mod.rs`、`src-tauri/src/commands/mod.rs`、`src/App.vue`：新增拍照状态覆盖能力，设置页可直接切换 `关闭覆盖 / 就绪 / 运行中 / 等待确认 / 失败 / 已完成`，只影响小组件当前显示，不写入事件历史。

### Mochi 固件适配
- `clawd_mochi.ino`：`routePetStatus()` 支持读取 `quota` 与 `quota2` 参数，并存入 `codexUsagePrimary`、`codexUsageSecondary`。
- `clawd_mochi.ino`：`drawCodexUsage()` 在宠物状态屏底部显示两行 Codex 余量。
- `clawd_mochi.ino`：`routeState()` 返回 `usagePrimary`、`usageSecondary`，便于 Web 控制页/调试端查看当前 Notice 同步到桌宠的余量。
- `clawd_mochi.ino`：运行中动画改为局部刷新，首次进入 `RUNNING` 画完整状态页，后续帧通过 `drawRunningPetFrame()` 只清理并重绘眼睛、嘴巴和动态点区域，降低 ST7789 全屏清屏导致的闪烁。

### 验证情况
- Notice 侧已在相关会话中通过 `cargo test`、`cargo check`、`pnpm build` 与 macOS 打包验证。
- Mochi 侧已安装并验证 `arduino-cli 1.5.1`，当前可用板卡配置为 `esp32:esp32:nologo_esp32c3_super_mini`，端口曾识别为 `/dev/cu.usbmodem1301`；固件编译曾通过，体积约 80% flash、11% RAM。
